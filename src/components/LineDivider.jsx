import React from "react";

const LineDivider = ({className}) => {
  return (
    <div className={`w-100 ${className}`} style={{height: 1, backgroundColor: "#12b886"}}></div>
  );
};

export default LineDivider;
